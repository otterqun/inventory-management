//const config = { 
    //  fps: 10, 
    //  qrbox: { width: 300, height: 150 },
    //  videoConstraints: { width: { ideal: 1280 }, height: { ideal: 720 }, facingMode: "environment" }
    //}

    const config = { 
      fps: 10, 
      // Guna ukuran kotak segi empat sama supaya kamera lebih fokus
      qrbox: { width: 250, height: 250 }, 
      aspectRatio: 1.0, 
      disableFlip: false // Boleh baca walaupun kau pegang barcode terbalik
    }